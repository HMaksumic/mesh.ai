{
  "name": "mesh-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "dev:web": "npm --workspace web run dev",
    "dev:api": "echo Run backend from apps/api (see README)",
    "build": "npm --workspace web run build"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18"
  }
}
